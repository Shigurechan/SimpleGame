
//キャンバスの大きさ
const CANVAS_WIDTH = 1000;
const CANVAS_HEIGHT = 800;


//minからmaxまでの整数を返す
function GetRandom(min,max)
{
      return Math.floor( Math.random() * (max + 1 - min) ) + min;
}


function VectorCrossPos(vectorA,vectorB)
{
      
}